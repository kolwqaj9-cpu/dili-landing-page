# PropKit 验证流程 - 详细步骤

## 🎯 验证方法

### 方法一：使用 PowerShell 脚本（最简单）

#### 步骤 1：打开 PowerShell
1. 按 `Win + X`，选择 **"Windows PowerShell"** 或 **"终端"**
2. 或者按 `Win + R`，输入 `powershell`，回车

#### 步骤 2：进入项目目录
```powershell
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
```

#### 步骤 3：运行启动脚本
```powershell
.\start_services.ps1
```

**如果提示"无法运行脚本"**，执行以下命令允许脚本运行：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```
然后再次运行：
```powershell
.\start_services.ps1
```

#### 步骤 4：验证服务状态
打开**新的 PowerShell 窗口**，运行：
```powershell
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
.\verify_flow.ps1
```

---

### 方法二：手动启动（如果脚本无法运行）

#### 窗口 1：启动 Cloudflared
1. 打开 **Anaconda Prompt** 或 **PowerShell**
2. 执行：
```powershell
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
cloudflared tunnel --config config.yml run 3090-Home
```
3. **保持这个窗口打开**，不要关闭

#### 窗口 2：启动 Python 后端
1. 打开**另一个** **Anaconda Prompt** 或 **PowerShell**
2. 执行：
```powershell
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
C:\ProgramData\Anaconda3\python.exe main.py
```
3. **保持这个窗口打开**，不要关闭

#### 窗口 3：运行验证脚本
1. 打开**第三个** **Anaconda Prompt** 或 **PowerShell**
2. 执行：
```powershell
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
C:\ProgramData\Anaconda3\python.exe e2e_full_verification.py
```

---

### 方法三：浏览器手动验证

#### 前提：确保两个服务都在运行
- ✅ Cloudflared 窗口显示 "Connected"
- ✅ Python 窗口显示 "🚀 后端已就绪，等待 Cloudflare 隧道连接..."

#### 步骤：
1. 打开浏览器，访问：`https://propkitai.tech/landing.html`
2. 点击 **"Request Access"** 按钮
3. 在弹出的窗口中输入邮箱（例如：`test@example.com`）
4. 点击 **"ACTIVATE FREE TRIAL"**
5. 等待几秒钟，系统会：
   - 调用 API
   - 触发 GPU 计算
   - 生成数据
   - 自动跳转到 dashboard
6. 在 dashboard 页面查看图表和数据

---

## 🔍 如何确认服务正常运行？

### 检查 Cloudflared
- 窗口应该显示类似：`Connection established`
- 没有错误信息

### 检查 Python 后端
- 窗口应该显示：`🚀 后端已就绪，等待 Cloudflare 隧道连接...`
- 当有请求时，会显示处理日志

### 检查 API 是否可访问
在浏览器中访问：`https://api.propkitai.tech/api/webhook`
- 如果显示 JSON 错误（这是正常的，因为需要 POST 请求）
- 如果显示"无法访问"，说明 Cloudflared 未正常运行

---

## ❓ 常见问题

### Q: PowerShell 提示"无法运行脚本"
**A:** 执行以下命令：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Q: 找不到 cloudflared.exe
**A:** 确保在正确的目录：`C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1`

### Q: Python 报错缺少模块
**A:** 在 Anaconda Prompt 中安装：
```powershell
C:\ProgramData\Anaconda3\python.exe -m pip install fastapi uvicorn requests pandas
```

### Q: API 无法访问
**A:** 
1. 检查 Cloudflared 是否运行
2. 检查 `config.yml` 配置
3. 等待几秒钟让隧道建立连接

---

## 📝 快速检查清单

- [ ] Cloudflared 窗口打开并显示 "Connected"
- [ ] Python 窗口打开并显示 "后端已就绪"
- [ ] 浏览器可以访问 `https://propkitai.tech/landing.html`
- [ ] 点击按钮后可以输入邮箱
- [ ] 提交后自动跳转到 dashboard
- [ ] Dashboard 显示数据或等待状态

---

## 🎬 完整验证流程视频说明

1. **打开 3 个窗口**：
   - 窗口 1：Cloudflared
   - 窗口 2：Python 后端
   - 窗口 3：验证脚本（可选）

2. **等待服务启动**（约 10-30 秒）

3. **打开浏览器测试**

4. **查看结果**

---

**提示**：如果遇到任何问题，先运行 `.\verify_flow.ps1` 查看详细状态！

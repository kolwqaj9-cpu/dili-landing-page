# 手动启动服务指南

如果自动启动脚本不工作，请按照以下步骤手动启动：

## 方法一：使用批处理文件（推荐）

**双击运行：`启动服务.bat`**

这会打开两个窗口，分别运行 Cloudflared 和 Python 后端。

---

## 方法二：完全手动启动

### 步骤 1：打开第一个 Anaconda Prompt 或 CMD 窗口

1. 按 `Win + R`
2. 输入 `cmd` 或 `powershell`，回车
3. 执行以下命令：

```cmd
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
cloudflared.exe tunnel --config config.yml run 3090-Home
```

**应该看到：**
```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable): |
|  https://api.propkitai.tech                                                                |
+--------------------------------------------------------------------------------------------+
```

**保持这个窗口打开！**

---

### 步骤 2：打开第二个 Anaconda Prompt 或 CMD 窗口

1. 再按 `Win + R`，打开**另一个**窗口
2. 执行以下命令：

```cmd
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
C:\ProgramData\Anaconda3\python.exe main.py
```

**应该看到：**
```
🚀 后端已就绪，等待 Cloudflare 隧道连接...
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000
```

**保持这个窗口也打开！**

---

## 验证服务是否运行

### 方法 1：检查进程

打开新的 CMD 窗口，运行：

```cmd
tasklist | findstr "cloudflared"
tasklist | findstr "python"
```

应该看到两个进程在运行。

### 方法 2：测试 API

在浏览器中访问：
```
https://api.propkitai.tech/api/webhook
```

如果看到 JSON 错误（这是正常的，因为需要 POST 请求），说明服务正常运行。

### 方法 3：运行检查脚本

```cmd
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1
check_services.bat
```

---

## 常见问题

### Q: Cloudflared 窗口立即关闭

**原因：** 配置文件错误或凭证文件缺失

**解决：**
1. 检查 `config.yml` 是否存在
2. 检查凭证文件：`C:\Users\Administrator\.cloudflared\86148ff9-84e3-44ad-ab47-e48805edf1fc.json`
3. 如果缺失，需要重新配置 cloudflared

### Q: Python 后端窗口报错

**常见错误：**
- `ModuleNotFoundError` - 缺少依赖
  - 解决：`C:\ProgramData\Anaconda3\python.exe -m pip install fastapi uvicorn requests`

- `Address already in use` - 端口被占用
  - 解决：关闭占用 8000 端口的程序，或修改 `main.py` 中的端口

### Q: 服务启动但无法访问

**检查：**
1. Cloudflared 窗口是否显示 "Connected"
2. Python 后端窗口是否显示 "Uvicorn running"
3. 防火墙是否阻止了连接
4. 域名 DNS 是否正确配置

---

## 快速启动命令（复制粘贴）

**窗口 1 - Cloudflared:**
```cmd
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1 && cloudflared.exe tunnel --config config.yml run 3090-Home
```

**窗口 2 - Python:**
```cmd
cd C:\Users\Administrator\Desktop\baseball\netlify_deploy\netlify_deploy1 && C:\ProgramData\Anaconda3\python.exe main.py
```

---

## 停止服务

直接关闭两个窗口即可。

或使用任务管理器：
1. 按 `Ctrl + Shift + Esc`
2. 找到 `cloudflared.exe` 和 `python.exe` 进程
3. 右键 → 结束任务
